/**
 * 配置文件
 * 包含所有应用配置常量
 */

const CONFIG = {
    ANIMATION: {
        COPY_PULSE_DURATION: 600,      // 复制动画持续时间(ms)
        TOAST_SHOW_DURATION: 2000      // 提示显示时间(ms)
    },
    UI: {
        POPUP_OFFSET: 10,              // 弹出框与边界的最小距离(px)
        SCROLL_BEHAVIOR: 'smooth'       // 滚动行为
    }
};
